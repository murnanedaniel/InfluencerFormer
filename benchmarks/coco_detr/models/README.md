# Model Notes

Planned contents:

- a thin adapter around a DETR-style baseline
- matcher injection points for Hungarian / PM3 / optional Sinkhorn
- reduced-data smoke configuration support

The first target is baseline parity, not a new detector architecture.
