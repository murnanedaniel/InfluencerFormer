# Config Templates

- `baseline_hungarian.json`: canonical DETR-style smoke baseline
- `pm3_softmatch.json`: first PM3-style matcher comparison on the same cost matrix

These are configuration templates for the future training implementation.
